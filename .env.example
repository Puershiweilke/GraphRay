# GraphRay 开发环境变量（模板）
# 用法：复制本文件为 .env 并填写真实值。.env 已被 .gitignore 忽略，永不入库。
# 注意：以下均为「开发期密钥」，仅用于本地/CI 工具链，绝不进入客户端构建包。

# GitHub Personal Access Token（仅用于 git 推送 / 发布；不要写进任何客户端代码）
GH_TOKEN=

# 部署服务器（仅工具链使用，如 deploy 脚本）
DEPLOY_HOST=
DEPLOY_USER=
DEPLOY_KEY_PATH=
