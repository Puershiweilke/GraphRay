{
  "_comment": "运行时配置模板（提交入库）。复制为 config.local.json 填写真实服务器地址，经 tools/inject-config.js 注入到 assets/resources/configs/runtime.json 后随构建打包。",
  "server": {
    "host": "127.0.0.1",
    "port": 3000,
    "protocol": "ws"
  },
  "env": "dev"
}
